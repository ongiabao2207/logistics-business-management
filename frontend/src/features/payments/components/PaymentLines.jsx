const formatVnd = (value) => `${new Intl.NumberFormat("vi-VN", { maximumFractionDigits: 0 }).format(Number(value))} ₫`;
export function PaymentLines({ lines, editable = false, values = {}, onChange }) { return <div className="pay-table-scroll"><table className="pay-table pay-lines"><thead><tr><th>Tên hạng mục / Dịch vụ</th><th>ĐVT</th><th>Số lượng xác nhận</th><th>Số lượng thanh toán</th><th>Đơn giá (VNĐ)</th><th>Thuế (%)</th><th>Thành tiền (VNĐ)</th></tr></thead><tbody>{lines.map((line) => <tr key={line.service_id}><td><strong>{line.description}</strong><small>{line.service_id}</small></td><td>Đơn vị</td><td>{Number(line.confirmed_quantity).toLocaleString("vi-VN")}</td><td>{editable ? <input className="pay-qty" type="number" min="0.0001" max={line.confirmed_quantity} step="any" value={values[line.service_id] ?? line.billing_quantity} onChange={(event) => onChange(line.service_id, event.target.value)} /> : Number(line.billing_quantity).toLocaleString("vi-VN")}</td><td>{new Intl.NumberFormat("vi-VN").format(Number(line.unit_price_snapshot))}</td><td>{Number(line.tax_rate) * 100}%</td><td><strong className="pay-money">{formatVnd(line.line_amount)}</strong></td></tr>)}</tbody></table></div>; }
