import { AlertCircle, CheckCircle2, Clock3, WalletCards } from "lucide-react";
import { formatCurrency } from "../../../shared/utils/formatters.js";
export function PaymentSummary({ payments }) { const cards = [{ label: "Tổng giá trị", value: formatCurrency(payments.reduce((sum, item) => sum + Number(item.total_amount), 0)), icon: WalletCards }, { label: "Đang chờ phê duyệt", value: `${payments.filter((item) => item.status === "PENDING_APPROVAL").length} hồ sơ`, icon: Clock3 }, { label: "Đã hoàn tất", value: `${payments.filter((item) => ["APPROVED", "SIGNED"].includes(item.status)).length} bảng`, icon: CheckCircle2 }, { label: "Cần điều chỉnh", value: `${payments.filter((item) => item.status === "REVISION_REQUESTED").length} bảng`, icon: AlertCircle }]; return <section className="payment-summary-grid">{cards.map(({ label, value, icon: Icon }) => <article className="payment-summary-card" key={label}><span className="summary-icon"><Icon size={20} /></span><div><span>{label}</span><strong>{value}</strong></div></article>)}</section>; }
