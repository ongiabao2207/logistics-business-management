[project]
name = "customer-service"
version = "0.1.0"
description = "Customer Service for the Logistics Business Management System"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "psycopg[binary]",
    "pyjwt[crypto]>=2.10,<3.0",
    "sqlalchemy",
    "uvicorn",
]

[project.optional-dependencies]
dev = [
    "httpx",
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
namespaces = true
